#include <stdio.h>


int main(void)
{
    double p, r, ur, amount;
    int t, first_time;

    scanf("%lf", &p);
    scanf("%lf", &r);
    scanf("%d", &t);
    
    scanf("%d", &ur);
    
    while (ur != -1)
    {
        
    }
}